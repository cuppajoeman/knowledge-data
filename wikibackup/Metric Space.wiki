A metric space is an ordered pair <math display="inline">\left( M, d \right)</math> where <math display="inline">M</math> is a set and <math display="inline">d</math> is a metric on <math display="inline">M</math>

{{Knowledge Metadata|Analysis|Definition}}